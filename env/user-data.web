#!/bin/bash

echo "Deadpool" > index.html
nohup busybox httpd -f -p 8080 &

# sudo yum update -y
# sudo yum install -y httpd24
# sudo yum install -y tomcat8
# sudo yum install -y ruby  
# sudo yum install -y aws-cli

# sudo service httpd start
# sudo service tomcat8 start

# sudo chkconfig httpd on
# sudo usermod -a -G tomcat ec2-user
# sudo usermod -a -G apache ec2-user
# sudo chown -R ec2-user:apache /var/www
# sudo chmod 2775 /var/www
# find /var/www -type d -exec sudo chmod 2775 {} \;
# find /var/www -type f -exec sudo chmod 0664 {} \;              
# sudo su
# cd /home/ec2-user
# curl -O https://aws-codedeploy-us-east-2.s3.amazonaws.com/latest/install
# chmod +x ./install
# ./install auto

# sudo echo "Hello, World" > /usr/share/tomcat8/webapps/index.html
# sudo usermod -a -G codedeploy-agent ec2-user
# sudo echo "Hello, World. User has been granted access." > /usr/share/tomcat8/webapps/index.html
# sudo service codedeploy-agent start
# sudo service httpd restart
